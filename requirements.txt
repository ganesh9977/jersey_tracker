# Jersey Tracker – Python Dependencies
# ======================================
# Install with:
#   pip install -r requirements.txt
#
# Note: PyTorch (required by EasyOCR) is installed automatically as a
# transitive dependency. If you have a GPU, install torch separately
# with the appropriate CUDA build from https://pytorch.org/get-started/locally/

flask>=2.3.0
opencv-python-headless>=4.8.0
easyocr>=1.7.0
numpy>=1.24.0
Pillow>=10.0.0

# Optional – only required when DETECTION_MODE=yolo (real cameras / IP streams)
# ultralytics>=8.0.0
